# Simple_tokio_app

Реограф **РКМ / РКМ-С**: приём COM / `.bin`, DSP, графики (egui).

Рабочий крейт и **подробная документация** (модули, поток данных, где делить на 1000):

→ **[`tokio_app/README.md`](tokio_app/README.md)**

```powershell
cd tokio_app
cargo run
cargo build --release
.\installer\build-installer.ps1
```
